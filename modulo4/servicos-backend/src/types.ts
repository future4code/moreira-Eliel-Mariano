export type user = {
   id: string
   name: string
   nickname: string
   email: string
   address: string
}

export type User = {
   CEP: string,
   Logradouro: string,
   Número: string,
   Complemento: string,
   Bairro: string,
   Cidade: string,
   Estado: string
}